# Abhängigkeiten (Dev-Toolchain)
node_modules/

# Coverage-Berichte (c8)
coverage/

# Playwright (nur lokal, bei Bedarf — NICHT in der CI)
test-results/
playwright-report/

# CI-Staging für GitHub Pages (nur Spiel-Dateien)
_site/

# OS-/Editor-Artefakte
.DS_Store
Thumbs.db
